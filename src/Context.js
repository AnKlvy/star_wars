import { createContext } from "react";

export const DefaultContex = createContext({});
export const FanContext = createContext({});
// export const FooterContext =createContext({});
export const AboutCon=createContext({});
export const AboutHandle=createContext({});
export const KeyCon=createContext({});
export const Add=createContext({});